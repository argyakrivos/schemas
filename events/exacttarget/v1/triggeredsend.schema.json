{
  "title": "The structure of a Triggered Send message to be sent to the Exact Target platform",
  "type": "object",
  "properties": {
    "$schema": {
      "title": "The type of event which has occurred",
      "type": "string",
      "pattern": "^events\.exacttarget\.triggeredsend\.v1"
    },
    "timestamp": {
      "title": "The timestamp at which this event occurred.",
      "type": "string",
      "format": "date-time"
    },
    "triggeredEmailName": {
      "title": "The triggered email template name to use.",
      "type" : "string"
    },
    "subscriber": {
      "title": "Details about the  Exact Target subscriber",
      "$ref": "common.schema.json#/definitions/subscriber"
    }
  },
  "additionalProperties": false,
  "required": ["$schema", "timestamp", "triggeredEmailName","subscriber"]
}